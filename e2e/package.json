{
  "name": "@payroll/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright browser E2E (hardening B) \u2014 drives the built SPA against the PGlite-backed server boot (@payroll/server e2e:serve).",
  "type": "module",
  "scripts": {
    "e2e": "playwright test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@better-auth/utils": "~0.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "~26.4.0",
    "typescript": "~5.9.3"
  }
}
